# Инструкция по настройке автоматического деплоя на FTP через GitHub Actions

## Что было сделано

✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
✅ Добавлен скрипт сборки в `package.json`

## Пошаговая настройка

### Шаг 1: Узнайте данные FTP-сервера

Вам понадобятся:
- **FTP_HOST** - адрес FTP сервера (например: `ftp.football.pavelsolntsev.ru` или IP адрес)
- **FTP_USERNAME** - имя пользователя FTP
- **FTP_PASSWORD** - пароль FTP
- **FTP_PATH** - путь на сервере, куда загружать файлы (например: `/public_html/` или `/`)

> 💡 **Совет**: Обычно FTP путь начинается с `/public_html/` или просто `/`, но уточните это у вашего хостинг-провайдера.

### Шаг 2: Уточните путь на FTP сервере

Откройте файл `.github/workflows/deploy.yml` и найдите строку:
```yaml
server-dir: /  # Или укажите нужную папку на сервере
```

Замените `/` на нужный путь. Например:
- `/public_html/` - для стандартного хостинга
- `/www/` - альтернативный вариант
- `/` - если корневая папка это и есть сайт

### Шаг 3: Настройте секреты в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **Settings** (Настройки)
3. В левом меню выберите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret** (Новый секрет репозитория)

Создайте 3 секрета:

#### Секрет 1: FTP_SERVER
- **Name**: `FTP_SERVER`
- **Secret**: ваш FTP хост (например: `ftp.football.pavelsolntsev.ru`)

#### Секрет 2: FTP_USERNAME
- **Name**: `FTP_USERNAME`
- **Secret**: ваш FTP логин

#### Секрет 3: FTP_PASSWORD
- **Name**: `FTP_PASSWORD`
- **Secret**: ваш FTP пароль

### Шаг 4: Запушьте изменения в GitHub

```bash
git add .
git commit -m "Add GitHub Actions deploy workflow"
git push origin master
```

### Шаг 5: Проверьте работу деплоя

1. После пуша перейдите на вкладку **Actions** в вашем репозитории GitHub
2. Вы увидите запущенный workflow "Deploy to FTP"
3. Нажмите на него, чтобы увидеть прогресс
4. Если всё настроено правильно, через 2-3 минуты файлы будут загружены на FTP

## Как это работает

1. При каждом пуше в ветку `master` автоматически запускается workflow
2. GitHub Actions:
   - Устанавливает Node.js
   - Устанавливает зависимости (`npm ci`)
   - Собирает проект (`gulp public`)
   - Загружает папку `public/` на FTP сервер

## Возможные проблемы

### Ошибка подключения к FTP
- Проверьте правильность FTP_HOST, FTP_USERNAME, FTP_PASSWORD
- Убедитесь, что FTP сервер доступен из интернета
- Проверьте, не требует ли хостинг пассивный режим FTP (обычно работает автоматически)

### Файлы загружаются не в ту папку
- Проверьте параметр `server-dir` в `.github/workflows/deploy.yml`
- Уточните у хостинг-провайдера правильный путь

### Ошибки при сборке
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что команда `gulp public` работает локально

## Дополнительные настройки

### Изменить ветку для деплоя
В файле `.github/workflows/deploy.yml` измените:
```yaml
branches:
  - master  # Замените на нужную ветку, например: main
```

### Исключить файлы из деплоя
В секции `exclude` добавьте паттерны файлов, которые не нужно загружать:
```yaml
exclude: |
  **/.git*
  **/node_modules/**
  **/.DS_Store
  **/temp/**
```


